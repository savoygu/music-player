// 音乐展示模式
export enum MODE {
  LIST = 'list',
  CARD = 'card'
}

export enum THEME {
  GREEN = 'green',
  ORANGE = 'orange'
}

// 播放模式
export enum PLAY_MODE {
  SEQUENCE = 0,
  LOOP = 1,
  RANDOM = 2
}
// 音乐存储方式
export enum STORAGE {
  LOCALE = 0,
  ONLINE = 1
}
